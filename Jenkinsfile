pipeline {
    agent any
    stages {
        stage('VM NodeJs Version') {
            steps {
                sh '''
                    node -v
                    npm -v
                '''
            }
        }
    }
}